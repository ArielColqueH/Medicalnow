import { PrescriptionDetailJSON } from './prescription-detail-json';

describe('PrescriptionDetailJSON', () => {
  it('should create an instance', () => {
    expect(new PrescriptionDetailJSON()).toBeTruthy();
  });
});
