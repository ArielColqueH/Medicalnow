import { QualificationModel } from './qualification-model';

describe('QualificationModel', () => {
  it('should create an instance', () => {
    expect(new QualificationModel()).toBeTruthy();
  });
});
